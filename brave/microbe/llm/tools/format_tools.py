import brave.api.service.analysis_service as analysis_service
from brave.api.config.db import get_engine
import os


async def format_tools(arguments: dict):
    # 这里写你自己的逻辑
    # data = f"模拟获取日志内容{biz_id}-{biz_type}"
    # print(f"获取日志: {biz_id}, {biz_type}")
    pass
    return """"
     format_tools
    """
    